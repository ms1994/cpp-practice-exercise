#include <bits/stdc++.h>

using namespace std;

int countSwap2(vector <int> *arr);
int countSwap(vector <int> *arr);
int main()
{
    int n;
    vector <int> arr;
    cin >> n;
    for (int i = 0; i < 2*n; i++)
    {
        int k;
        cin >> k;
        arr.push_back(k);
    }
    int a = countSwap2(&arr);
    for (auto it : arr)
        cout << it << " ";
    return 0;
}
int countSwap(vector <int> *arr)
{

}
